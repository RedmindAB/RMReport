{
	suite:[1],
	class:[1],
	driver:['iPhone 4','HTC one']
}


SELECT timestamp, result, report.time FROM report WHERE

if suite.size > 0

if driver.size > 0;

SELECT timestamp, result, report.time FROM report WHERE driver = 'iPhone 4'

if driver.next()

SELECT timestamp, result, report.time FROM report WHERE driver = 'iPhone 4' OR driver = 'HTC one'
